# Nuphus 中继服务使用边界与用途限制（Relay Usage Policy）

> 本文件定义 Nuphus 官方中继节点（免费提供）的服务边界、允许用途与禁止用途。
> 对用户是使用规则；对项目方是服务性质声明。
> 适用于：Nuphus 桌面端（`relay_client`）、手机端 PWA、`relay-server` 中继服务及其全部派生部署。

---

## 一、中继服务定位

Nuphus 中继服务（`relay-server`）是**远程控制功能的技术组成部分**，为手机端提供异地访问桌面 Nuphus 的公网通道：

- 桌面端建立**出站长连接**接入中继，手机端经中继访问桌面；
- 无需公网 IP、无需端口映射；
- 中继**不落盘存储内容**，只做身份校验与转发路由；
- 协议为 **Nuphus 专用协议**（`device_id` + token 鉴权的 WS 隧道），**不是通用代理/VPN**，不能作为浏览器或系统级上网通道使用。

**重要区分**：本中继是「特定应用的数据通道」（与 TeamViewer / 向日葵等远程控制软件的服务器中转同类），不是「互联网接入服务」或「翻墙代理」。

## 二、允许的用途

1. **远程控制/访问用户自有设备**：手机端远程操控自己的桌面 Nuphus（聊天、任务、文件访问等）。
2. **局域网直连不可达时的兜底**：用户离开同一 WiFi 后，通过中继访问自家桌面。
3. **自建节点**：用户自行部署 `relay-server` 实例，服务自身或自有设备组。

## 三、禁止的用途（红线）

以下用途一律禁止，发现即终止服务：

1. **作为上网代理/翻墙通道**：将中继用于访问境外网站、规避网络访问限制，或任何形式的通用网络代理。
2. **转售/共享接入**：将中继接入资格、token、隧道通道转售、出租或提供给未授权第三方。
3. **非法用途**：利用中继传输违法内容、实施网络攻击、侵犯他人隐私或从事任何违反适用法律的活动。
4. **超范围使用**：将专用协议通道用于传输非 Nuphus 协议数据，或改造中继为通用转发服务。

## 四、技术边界声明

- 中继只做身份校验（device token / caller token）与路由，**不落盘、不存储、不审查内容**；
- 中继不提供内容缓存、不提供境外内容访问能力、不承载通用 IP 流量；
- 中继仅服务于 Nuphus 项目用户（账号/设备绑定），不向不特定公众开放注册。

## 五、服务性质声明

- 官方中继节点为 **Nuphus 开源项目的附属便利**，**免费提供、非经营性**；
- 项目方保留对官方中继节点的**接入控制、限流、暂停与终止**权利，以维护服务安全与合规边界；
- 项目方不对自建中继节点的可用性、安全性及合规性负责——自建节点由部署者自行管理并遵守当地法律。

## 六、用户责任

- 用户应仅将中继用于**控制自有设备**的合法目的；
- 用户知悉并同意：滥用中继（尤其用于访问境外内容/作为通用代理）可能导致服务被终止，并可能使使用者自身承担相应法律责任；
- 用户使用中继产生的数据流向（含跨地域传输），由用户知悉并自主决定。

## 七、变更与终止

- 本政策可能随项目发展更新，更新后于仓库发布即生效；
- 项目方有权在发现红线用途时立即终止相关接入，无需另行通知。

---

*本文件为服务边界声明，不构成法律意见。具体合规问题请咨询专业律师。*